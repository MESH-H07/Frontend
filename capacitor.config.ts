import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.ho7.familiar",
  appName: "Familiar",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
